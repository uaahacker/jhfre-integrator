| Item | Current state | Priority | Recommendation |
|---|---|---:|---|
| Metadata URL import | XML paste/import already works | Medium | Add later with strict SSRF-safe fetching |
| SLO bindings beyond Redirect | Redirect logout supported | Low/Medium | Add only when a real IdP requires POST SLO |
| Encrypted SAML assertions | Signed assertions supported, encrypted assertions not | Medium | Add when targeting IdPs that mandate assertion encryption |
| Immutable attribute identity | Feature exists, but admin must know the correct immutable claim | Low | Improve UI/help/metadata guidance later; no security shortcut |